# Textures

- `grain.svg`: fractal-noise tile. It's fixed over the whole page (`body::before` in bundle.css) at 3.5% opacity with `mix-blend-mode: overlay`, tiled at 180px. It's what makes flat fills read as material. Don't raise the opacity.
